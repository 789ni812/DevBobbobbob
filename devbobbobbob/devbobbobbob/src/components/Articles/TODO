# Articles:
    [ ] Add Date created and date revised
    [ ] Author
    [x] add external resource field to article content type in drupal
    [ ] Add external resource field to articles render
    [ ] add image //FIXME corbs bug
    [x] @FIXME check if article body exists before rendering
    
    # external artcile 
    [x] add external field in drupal 
    @log added in drupal: "external_resource	field_external_resource	Boolean"
    [ ] add if external article do .... else (based on layout or unique action)

    ## style:
        [ ] Create Article list with images 
        [ ] Create full article display
        
    # Testing:
        [ ] Update Tests