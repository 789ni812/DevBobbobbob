# MarkDown Renderer:
scanning the local filesystem is not practical and either not possible or difficult to do with Create React App (research this more though eh if you need a solution)

Decided reading markdown files from the local filesystem is redundant now we have Drupal as a backend 